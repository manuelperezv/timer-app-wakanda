"use client";
import React from 'react';
import {LocationButtonCmpWrapper} from './LocationButtonCmp.style';

export type LocationButtonCmpProps = {
	// types...
}

const LocationButtonCmp: React.FC<LocationButtonCmpProps>  = ({}) => {
	return (
		<LocationButtonCmpWrapper>
 			LocationButtonCmp works!
 		</LocationButtonCmpWrapper>
	);
};

export default LocationButtonCmp;
